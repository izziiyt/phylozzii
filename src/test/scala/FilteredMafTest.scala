import org.scalatest.FunSuite

class FilteredMafTest extends FunSuite {
  /*test("filter test"){
    val mafunits = FilteredMafParser("src/test/resources/data.filtered.maf")
    val xs = mafunits.map(u => {val x = u.columns.map(s => s.length);x.forall(_ == x(0))})
    assert(xs.forall(_ == true))
  }*/
}
